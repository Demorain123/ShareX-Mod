name: LongCapture Browser Agent v0.1.3 Quality Region

on:
  push:
    branches:
      - "standalone/browser-agent-v0.1-poc"
    paths:
      - "LongCapture.Standalone/**"
      - "ShareX.ScreenCaptureLib/**"
      - "ShareX.Avalonia/**"
      - "mod-overlay/**"
      - "scripts/**"
      - ".github/workflows/browser-agent-v013-quality-region.yml"
  pull_request:
    branches:
      - "standalone/longcapture-v0.1.9-rc6-sparse-edge-evidence"
    paths:
      - "LongCapture.Standalone/**"
      - "ShareX.ScreenCaptureLib/**"
      - "ShareX.Avalonia/**"
      - "mod-overlay/**"
      - "scripts/**"
      - ".github/workflows/browser-agent-v013-quality-region.yml"
  workflow_dispatch:

concurrency:
  group: browser-agent-v013-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build-and-test:
    runs-on: windows-2025
    timeout-minutes: 55
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Apply RC6 overlay chain
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $hooks = @(
            "scripts\run-v04-hooks.ps1",
            "scripts\apply-v041-post-hooks.ps1",
            "scripts\apply-v042-post-hooks.ps1",
            "scripts\apply-v043-post-hooks.ps1",
            "scripts\apply-v044-post-hooks.ps1",
            "scripts\apply-v050-post-hooks.ps1",
            "scripts\apply-v051-post-hooks.ps1",
            "scripts\apply-v052-post-hooks.ps1",
            "scripts\apply-v061-post-hooks.ps1",
            "scripts\apply-v062-post-hooks.ps1",
            "scripts\apply-v064-post-hooks.ps1",
            "scripts\apply-longcapture-v013-standalone-hooks.ps1",
            "scripts\apply-longcapture-v013-rc-hardening.ps1",
            "scripts\apply-longcapture-v014-core-hooks.ps1",
            "scripts\apply-longcapture-v015-fixed-overlay-hooks.ps1",
            "scripts\apply-longcapture-v016-replay-compositor-hooks.ps1",
            "scripts\apply-longcapture-v017-anchor-continuity-hooks.ps1",
            "scripts\apply-longcapture-v018-precut.ps1",
            "scripts\apply-longcapture-v018-trust-split-hooks.ps1",
            "scripts\apply-longcapture-v018-rc-evidence.ps1",
            "scripts\apply-longcapture-v019-recovery-tail-hooks.ps1",
            "scripts\apply-longcapture-v019-rc3-real-evidence-hooks.ps1",
            "scripts\apply-longcapture-v019-rc4-upward-alias-veto.ps1",
            "scripts\apply-longcapture-v019-rc5-downward-alias-consensus.ps1",
            "scripts\apply-longcapture-v019-rc6-sparse-edge-evidence.ps1",
            "scripts\apply-longcapture-v019-rc6-strong-direct-order.ps1"
          )
          foreach ($hook in $hooks) {
            Write-Host "=== $hook ==="
            & pwsh -NoProfile -File $hook
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

      - name: Apply Browser Agent v0.1 -> v0.1.3 overlays
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          foreach ($hook in @(
            "scripts\apply-browser-agent-v01-poc.ps1",
            "scripts\apply-browser-agent-v012-integration.ps1",
            "scripts\apply-browser-agent-v013-main-ui.ps1",
            "scripts\apply-browser-agent-v013-quality-core.ps1",
            "scripts\apply-browser-agent-v013-core-followup.ps1"
          )) {
            Write-Host "=== $hook ==="
            & pwsh -NoProfile -File $hook
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
          git diff --check

      - name: Verify v0.1.3 source and permission invariants
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $main = Get-Content "LongCapture.Standalone\MainForm.cs" -Raw
          $session = Get-Content "LongCapture.Standalone\BrowserAgentCaptureSession.cs" -Raw
          $stitcher = Get-Content "LongCapture.Standalone\BrowserAgentStreamingPngStitcher.cs" -Raw
          $worker = Get-Content "LongCapture.Standalone\BrowserAgent\Extension\service-worker.js" -Raw
          $manifest = Get-Content "LongCapture.Standalone\BrowserAgent\Extension\manifest.json" -Raw | ConvertFrom-Json
          foreach ($marker in @("BrowserAgentUiModeAdapterV013", "Browser Agent v0.1.3 options", "BuildOptions")) {
            if (-not $main.Contains($marker)) { throw "Missing v0.1.3 main UI marker: $marker" }
          }
          foreach ($marker in @("BrowserAgentCaptureOptions", '"selectRegion"', '"preload"', "BrowserAgentFrameCropper", "ResolvedDeltaPixels")) {
            if (-not $session.Contains($marker)) { throw "Missing v0.1.3 capture marker: $marker" }
          }
          foreach ($marker in @("chooseCaptureRegion", "preloadDynamicContent", "captureRegion", "const captureHeight")) {
            if (-not $worker.Contains($marker)) { throw "Missing v0.1.3 extension marker: $marker" }
          }
          if (-not $stitcher.Contains("int resolvedDelta = frame.ResolvedDeltaPixels")) { throw "Visual cumulative stitch marker missing" }
          if ($manifest.version -ne "0.1.3") { throw "Extension manifest is not v0.1.3" }
          $permissions = @($manifest.permissions)
          foreach ($permission in @("activeTab", "scripting", "nativeMessaging")) {
            if ($permissions -notcontains $permission) { throw "Missing permission $permission" }
          }
          if ($permissions -contains "debugger" -or $manifest.host_permissions) { throw "Browser Agent permission boundary expanded unexpectedly" }

      - name: Restore
        run: dotnet restore --runtime win-x64 LongCapture.Standalone\LongCapture.Standalone.csproj

      - name: Publish portable
        run: dotnet publish LongCapture.Standalone\LongCapture.Standalone.csproj --no-restore --configuration Release --runtime win-x64 --self-contained true --output artifacts\LongCapture /m:1

      - name: Browser Agent v0.1.3 deterministic self-test
        shell: cmd
        run: artifacts\LongCapture\LongCapture.exe --browser-agent-poc-self-test

      - name: Existing RC6 QUICK acceptance
        shell: cmd
        run: artifacts\LongCapture\1-RUN-AUTOMATED-TESTS.cmd --ci

      - name: Verify packaged v0.1.3 boundary
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          foreach ($path in @(
            "artifacts\LongCapture\LongCapture.exe",
            "artifacts\LongCapture\BrowserAgent\Extension\manifest.json",
            "artifacts\LongCapture\BrowserAgent\Extension\service-worker.js",
            "artifacts\LongCapture\BrowserAgent\INSTALL-BROWSER-AGENT.cmd"
          )) {
            if (-not (Test-Path -LiteralPath $path)) { throw "Missing packaged file: $path" }
          }
          $manifest = Get-Content "artifacts\LongCapture\BrowserAgent\Extension\manifest.json" -Raw | ConvertFrom-Json
          if ($manifest.version -ne "0.1.3") { throw "Packaged extension is not v0.1.3" }
          $worker = Get-Content "artifacts\LongCapture\BrowserAgent\Extension\service-worker.js" -Raw
          foreach ($marker in @("chooseCaptureRegion", "preloadDynamicContent", "captureRegion")) {
            if (-not $worker.Contains($marker)) { throw "Packaged worker missing $marker" }
          }

      - name: Package v0.1.3
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          New-Item -ItemType Directory -Force Output | Out-Null
          $zip = "Output\LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.zip"
          Compress-Archive -Path "artifacts\LongCapture\*" -DestinationPath $zip -CompressionLevel Optimal -Force
          $hash = (Get-FileHash -Algorithm SHA256 $zip).Hash.ToLowerInvariant()
          Set-Content "Output\LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.sha256.txt" "$hash  LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.zip"

      - name: Extract final ZIP and rerun Browser Agent self-test
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $temp = Join-Path $env:RUNNER_TEMP "longcapture-browser-agent-v013-final"
          Remove-Item $temp -Recurse -Force -ErrorAction SilentlyContinue
          Expand-Archive "Output\LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.zip" $temp -Force
          $process = Start-Process `
            -FilePath (Join-Path $temp "LongCapture.exe") `
            -ArgumentList "--browser-agent-poc-self-test" `
            -WorkingDirectory $temp `
            -Wait `
            -PassThru
          if ($process.ExitCode -ne 0) { throw "Extracted v0.1.3 self-test failed with exit code $($process.ExitCode)" }

      - name: Upload v0.1.3 package
        if: success()
        uses: actions/upload-artifact@v7
        with:
          name: LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64
          path: |
            Output\LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.zip
            Output\LongCapture-Browser-Agent-v0.1.3-Quality-Region-x64.sha256.txt
          if-no-files-found: error
