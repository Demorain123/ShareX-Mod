# Building

CI is the source of truth for the standalone Windows x64 package. Local build: `dotnet publish LongCapture.Standalone/LongCapture.Standalone.csproj -c Release -r win-x64 --self-contained true`.
